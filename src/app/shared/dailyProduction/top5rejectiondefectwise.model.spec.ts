import { Top5rejectiondefectwise } from './top5rejectiondefectwise.model';

describe('Top5rejectiondefectwise', () => {
  it('should create an instance', () => {
    expect(new Top5rejectiondefectwise()).toBeTruthy();
  });
});
