import { TestBed } from '@angular/core/testing';

import { Top5rejectiondefectwiseService } from './top5rejectiondefectwise.service';

describe('Top5rejectiondefectwiseService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: Top5rejectiondefectwiseService = TestBed.get(Top5rejectiondefectwiseService);
    expect(service).toBeTruthy();
  });
});
